# Public contact values already have the approved defaults in src/data/sitio.ts.
NEXT_PUBLIC_SITE_URL=https://alaryfes.com
NEXT_PUBLIC_WHATSAPP=34623173625
NEXT_PUBLIC_CONTACT_EMAIL=hola@alaryfes.com

# Set these only if the legal holder changes.
# NEXT_PUBLIC_LEGAL_NAME=
# NEXT_PUBLIC_LEGAL_NIF=
# NEXT_PUBLIC_LEGAL_ADDRESS=

# Server-only endpoint that accepts JSON and writes the lead to Google Sheets.
# Without it the API logs the request and returns HTTP 202 pending_configuration.
CONTACT_WEBHOOK_URL= 
CONTACT_WEBHOOK_TOKEN=

# Optional. No third-party tracking is loaded until the visitor opts in.
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_META_PIXEL_ID=
